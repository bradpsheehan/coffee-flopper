<h1>Listing shops</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Website</th>
    <th>Phone</th>
    <th>Wifi</th>
    <th>Groups</th>
    <th>Street</th>
    <th>City</th>
    <th>show</th>
    <th>edit</th>
    <th>destroy</th>
  </tr>

<% @shops.each do |shop| %>
  <tr>
    <td><%= shop.name %></td>
    <td><%= shop.website %></td>
    <td><%= shop.phone %></td>
    <td><%= shop.wifi %></td>
    <td><%= shop.groups %></td>
    <td><%= shop.address.line1 %></td>
    <td><%= shop.address.city %></td>
    <td><%= link_to 'Show', shop %></td>
    <td><%= link_to 'Edit', edit_shop_path(shop) %></td>
    <td><%= link_to 'Destroy', shop, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shop', new_shop_path %>
