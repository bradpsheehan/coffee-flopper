<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <div class="nav-collapse collapse">
      <ul class="nav">
        <li>
          <%= link_to "COFFEE FLOPPER", root_path %>
        </li>
        <li>
          <%= link_to 'New Shop', new_shop_path %>
        </li>
        <% if @shops %>
        <li class="active">
          <a href="#">
            Floppers
            <span class="navbar-unread"><%= @shops.count %></span>
          </a>
          <ul>
            <% @shops.each do |shop| %>
            <li><%= link_to shop.name, shop_path(shop) %></li>
            <% end %>
          </ul> <!-- /Sub menu -->
        </li>
        <% end %>
        <li><%= link_to "About Us", "https://github.com/blairand/coffee-flopper"%>
        </li>
      </ul>
      <form class="navbar-form">
        <input class="span4" type="search" placeholder="Sippin... Searchin....">
        <button type="submit" class="btn">Search</button>
      </form>
    </div><!--/.nav-collapse -->
  </div>
</div>
